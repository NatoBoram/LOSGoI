package self

import "runtime"

// Metadata
const (
	User    = "LOSGoIBot"
	Version = "0.1.0"
	Contact = "https://gitlab.com/NatoBoram/LOSGoI"
	OS      = runtime.GOOS
	Arch    = runtime.GOARCH
)

// Metadata
var (
	Go = runtime.Version()
)
